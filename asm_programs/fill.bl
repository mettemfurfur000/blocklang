    add 7       ; add 7s to acc
    push ACC    ; push from acc onto stack
    jof next    ; if full, go next
    jmp NIL     ; loop until full
next:
    pop DOWN     ; pop one value down
    get SLN     ; check stack length
    jnz next    ; repeat popping
    halt
